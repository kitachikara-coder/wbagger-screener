# CLAUDE.md — wbagger-screener（ストップ高後の押し目スクリーニング）

東証グロース＋スタンダードを対象に、**直近20営業日にストップ高をつけた銘柄**を毎営業日ぜんぶ並べ、
「押し目がどこまで熟したか」を事実の列だけで見せるダッシュボード。
**買うべきかを機械に決めさせない。** 本人が「この銘柄を今、自分なら買うか」を判断するための
材料を最適な形で揃えるのが目的。合成スコア・◎○△の総合判定は作らない。

このファイルは Claude Code / Codex がセッションをまたいで作業を継続するための出発点。
**会話の文脈は引き継がれないので、状態はここに書くこと。**

> 親プロジェクト kabu-monitor（`../`）とは別リポジトリ。あちらは日本株スイングの
> テーマ別監視で、`../CLAUDE.md` に別の引き継ぎ書がある。
> ⚠ **ローカルでは `kabu-monitor` リポジトリのサブディレクトリとして存在している**
> （`git rev-parse --show-toplevel` は親を返す）。GitHub 側は別リポジトリなので、
> 変更を公開するときは同期の仕方を人に確認すること。

---

## リポジトリ構成

| ファイル | 役割 |
|---|---|
| `screener.py` | 本体。母集団抽出 → 指標計算 → `docs/index.html` と `docs/data/latest.json` を生成 |
| `criteria.yaml` | 閾値。**既存キーは削除しない**（後方互換。`market_name` 単体指定のフォールバックも維持） |
| `manual/{code}.yaml` | 手入力（材料・材料分類・継続性・決算日・浮動株）。`_template.yaml` が雛形 |
| `docs/data/decisions.json` | 判断ログ。GitHub 上で人が追記する |
| `backtest.py` | S高シグナル × フィルタ別の「+20%到達」検証（グロースのみ・出力 `docs/backtest.html`） |
| `backtest_dip.py` | **層Aの既定フィルタを解禁してよいか**を決める検証（4節・出力 `docs/backtest_dip.html`） |
| `tests/` | pytest。ネット不要（APIは全部モック） |
| `.github/workflows/screen.yml` | 平日18:30 JST 自動実行（`cron: "30 9 * * 1-5"`）→ `docs/` をコミット |
| `.github/workflows/backtest.yml` / `backtest_dip.yml` | 手動実行のみ（重いので自動化しない） |
| `docs/handover/Code依頼_*.md` | 実装の契約 |
| `docs/設計検討_*.md` | 設計根拠（依頼書と対で読む） |

## 実行方法
- ローカル: `JQUANTS_API_KEY=... python3 screener.py`
- CI: GitHub Actions が Secrets から渡す
- テスト: `python3 -m pytest tests/ -q`（各ファイルは `python3 tests/test_xxx.py` でも走る）
- Secrets名: **`JQUANTS_API_KEY`**。APIキーはコードに直書きしない
- 公開URL: https://kitachikara-coder.github.io/wbagger-screener/ （GitHub Pages / `main` の `/docs`）
- 契約プラン: **Standard**（`/markets/margin-interest` が 200 で返ることを 2026-09-12 に実測）

---

## 凍結事項（変更禁止・最初に読む）

- **指標計算関数は1バイトも変えない**: `sma` / `sma_series` / `ema_series` / `macd_series` /
  `rci_series` / `build_series` / `resample_weekly`。`tests/test_frozen.py` が AST で
  切り出した SHA256 を固定して検知する。**ハッシュが変わったら、変えてよい変更かを人が判断してから**値を更新する
- **`JQuants` クライアントの挙動も凍結**: v2 APIキー方式（`x-api-key`）、`min_interval=1.05` 秒の
  スロットル、`data` 配列＋`pagination_key` のページング。Standard は 120req/分だが
  **当面 1.05 秒のまま据え置く**（変更は別依頼）
- **チャート描画は凍結**: ローソク足（wick/body の floating bar）・日足/週足トグル・MA5/25/75・
  MACD・RCI9/26 のレイアウトとJS。列や指標の増減で壊さない。
  行の描画とカードは**別スクリプト**に分けてあるので、そちらを触ること
- **`backtest.py` / `backtest_dip.py` の検証ロジックは凍結**: 特に `bracket()` の
  「同日両触れは損切り優先」「往復コスト0.25%」「翌営業日始値エントリー」。
  `tests/test_frozen.py` がこの3点を実際に走らせて確認する
- **合成スコア（M2・押し目スコア相当）を新設しない**。親プロジェクトで
  「現行総合スコアは選別力なし」「M2 は逆相関」が確定済み。同じ轍を踏まない
- `docs/index.html` と `docs/data/latest.json` は **Actions が生成してコミットする**。手で編集しない
  （ローカル確認で上書きしたら `git checkout -- docs/` で戻す）

---

## J-Quants v2 の確認済み事実（推測で書かない）

実測日 2026-09-12。列名は**レスポンスそのまま**。

| エンドポイント | 実測した列 | 備考 |
|---|---|---|
| `/equities/bars/daily?date=` | `Code` `Date` `O` `H` `L` `C` `UL` `LL` `Vo` `Va` `MktCap` `AdjFactor` `AdjO` `AdjH` `AdjL` `AdjC` `AdjVo` `ExRT` | 1日分で全銘柄 約4,447行を**1リクエスト**（ページング無し） |
| `/equities/bars/daily?code=&from=&to=` | 同上 | 銘柄別の期間取得。`UL` も入る |
| `/equities/master` | `CoName` `CoNameEn` `Code` `Date` `Mkt` `MktNm` `Mrgn` `MrgnNm` `ProdCat` `S17` `S17Nm` `S33` `S33Nm` `ScaleCat` | 4,447行。**`S33Nm` は半角中黒**（「情報･通信業」）で返ることがあるので `normalize_sector()` を通す |
| `/markets/margin-interest` | `Date` `Code` `IssType` **`LongVol`**(買残) **`ShrtVol`**(売残) `LongStdVol`/`ShrtStdVol`(制度) `LongNegVol`/`ShrtNegVol`(一般) | **Standard以上**。週次で `Date` は金曜。`code`/`from`/`to` が効く。4桁・5桁どちらのコードでも引ける |
| `/fins/summary` | `CurPerType` `DiscDate` `Sales` `OP` `NP` `Eq` `EqAR` `CFO` `FOP` `ShOutFY` `TrShFY` | `EqAR` は比率なので×100 |
| `/equities/earnings-calendar` | `CoName` `Code` `Date` `FQ` `FY` `Section` `SectorNm` | ⚠ **`date`/`from`/`to`/`code` を全て無視し、翌営業日発表分だけを返す限定フィード**。前向きカレンダーではない。2026-09-12 実測で**受信1件**。「収録が無い＝発表予定なし」ではない |

- `UL == "1"` は **「日通し高値が制限値幅上限に達した」**＝*触れた*であって、
  ストップ高で引けたという意味ではない。実際 2026-09-11 の当日S高6件のうち
  4440 は終値が当日高値の -14.5%、6081 は -19.3% だった
- **コードは5桁**（`13010` / `338A0`）。表示は `disp_code()` で4桁に畳む（`338A`）。
  `manual/` のファイル名も4桁。
  **`/equities/bars/daily` と `/markets/margin-interest` は4桁でも5桁でも引ける**
  （2026-09-12 実測: `7810`/`78100`/`338A`/`338A0` の4通りすべて 200・同じ行が返る）
- ⚠ **日付一括スナップショット同士で前日比を出してはいけない**。権利落ち・分割の当日は
  生値 `C` が飛ぶ。**正しい式は `C_t ÷ (C_p × AdjFactor_t) − 1`**（2026-09-12 実測:
  14470 は生値同士だと **-3.51%**、正しい前日比は **+44.65%**、本式で **+44.63%**）。
  `AdjFactor` が取れない行は数字を作らず母数から外す
- **日証金の貸借倍率は非配信**。信用倍率（買残÷売残）で代替し、UIに「別物」と注記してある
- **浮動株比率も非配信**。`manual/{code}.yaml` の `float_shares` を手入力した銘柄だけ表示

---

## 進行中タスク（2026-09-12 時点）

| # | タスク | 状態 | メモ |
|---|---|---|---|
| P1 | 層A（ふるい一覧）の列と母集団 | **実装済み・人の確認待ち** | `recent_stop_high()` / `dip_metrics()` / `turnover_oku20()`。判定◎○△は撤去済み |
| P2 | 層B（銘柄カード） | **実装済み・人の確認待ち** | `fetch_margin()` で G2（信用残の列名誤り）も修正 |
| P3 | 判断ログ | **実装済み・人の確認待ち** | `docs/data/decisions.json` に人が追記する運用。まだ0件 |
| V0 | **B群ゲート**（既定フィルタの解禁可否） | **未実行** | `backtest_dip.py` は実装済みだが**一度も走らせていない**。`docs/backtest_dip.html` は存在しない |
| — | `manual/{code}.yaml` の入力 | **未着手** | `_template.yaml` だけ。**材料分類は全銘柄「未」**、決算日も全銘柄 `—` |
| — | 浮動株比率の入力 | 未着手 | 四季報から手入力。入れると「買残÷浮動株」が出る |

### 次にやること（優先順）
1. **`backtest_dip` を Actions から1回走らせる**（グロース＋スタンダード約2,100銘柄・40分前後）。
   合格するまで `criteria.yaml: dip_default_filter` は `false` のまま
2. ゲート通過候補の `manual/{code}.yaml` を書く（材料・分類・継続性・決算日）
3. 判断ログを2週間ためてから、列の増減を検討する

---

## 重要な検証結果（判断の前提・上書きしないこと）

### このリポジトリで**まだ**分かっていないこと
- **`docs/backtest.html`（S高シグナル × フィルタ別の +20%到達率）は未生成**。
  `backtest.py` は実装済みだが実行記録が無い
- **`docs/backtest_dip.html`（S高後の押し目のブラケット検証）も未生成**。
  したがって **「下落率 -10〜-20% かつ 枯れ比 ≤30%」に選別力がある根拠は無い**。
  層Aはこの2列を**表示するだけ**で、既定では絞らない
- 合格条件（依頼書 V0）: **+5%到達率と平均損益の両方**が、
  ①同ユニバース無選別ベンチ と ②「S高後1〜20日の全日」ベンチ の**両方**を上回ること。
  ②も超えないと「S高後であること」自体の効果と切り分けられない。
  感度表で主条件だけ良く前後セルが悪ければ**ナイフエッジ＝偶然を疑い、中庸の値を採る**

### 親プロジェクト kabu-monitor で確定済み（同じ轍を踏まないため）
- **8週バックテストで M2 は逆相関**: M2≥95 が -2.21%、M2<70 が +0.22%。
  **「直近で最も上げた順」は「下げ局面で最も売られる順」でもある**
- **セクターRS上位帯も寄付成行でマイナス**（テーマ版 -1.32%）
- **シグナル時点の地合いが良い週ほど翌週が悪い**（全市場ブレッドスで層別・両版一致）
- **押し目指値は「安値+0.5%以内の際どい約定」を不成立にすると符号が反転する**セルがある
  （-4%指値: +0.19% → -0.37%）。約定ガード後で読むこと
- → **出来高枯れ比・下落率・セクター連動にも同じ罠がありうる【推測】**。
  検証を通すまで**表示のみ**で、抽出条件・並び順の根拠・配点には入れない
- ベンチマークは **TOPIX ではなく同ユニバース（グロース＋スタンダード）の無選別**にする
- 禁じ手: 単一期間での重み最適化 ／ ベンチを TOPIX にすること ／ 学習期間だけで採否を決めること

### 実データの実測（2026-09-11 断面・ローカル1回・P1〜P3 通し）
- 母集団 **90件**（当日S高 6件 / 押し目進行中 84件）。グロース55 / スタンダード35
- **APIリクエスト 論理293回**（抽出まで23＝S高探索20＋master1＋取引日2 ／ 銘柄別270＝90×3）・**343秒**
- 生成物: `index.html` 3.7MB（98%がチャート系列）／ `latest.json` 301KB
- 枯れ比10%未満が34件。最も枯れていたのは 7810 クロスフォー（S高 8/26・高値-28.3%・枯れ比0.2%）
- **信用残は90/90で取得できた**（G2修正前は 0/90 が `null` だった）
- ⚠ **90銘柄のうち45銘柄は売残0** → 信用倍率が出せない。
  親プロジェクトの `swing_analyzer.fetch_margin_detail` は**売残0の週を丸ごと捨てる**実装なので、
  同じ書き方をするとこのユニバースでは**半数の銘柄で買残まで消える**。
  こちらは週を残して倍率だけ `None` にしてある（`parse_margin`）
- **決算日まで は全90件が `—`**（earnings-calendar が翌営業日分しか返さず、手入力もまだ無いため）
- タブー該当 27/90件。財務が取れない銘柄が16件（新規上場等）
- **当日S高6件のうち2件は終値が当日高値から二桁下**（4440 -14.5% / 6081 -19.3%）。
  依頼書は「当日S高は下落率≈0」と想定していたが、`UL=1` は *触れた* の意味なので
  そうとは限らない。むしろ「S高に触れたが引けで戻された」が一覧で見分けられる

---

## 実装上の規約

- **列の意味が変わる変更は必ずテストを足す**。`tests/` はネット不要（APIは全部モック）で、
  `python3 -m pytest tests/ -q` が受け入れ条件
- **`—`（U+2014）は「データ取得不可・算出不能」**、**「未」は手入力待ち**。取り違えない。
  特に **`None` を `0%` と表示しない**（分母0の枯れ比、決算日未設定など）
- **タブー列は3値**: 財務が取れていない=`—` / 該当なし=空欄 / 該当=理由
- 仮説由来の解釈文には必ず **【推測】** を付ける。測定値そのものには付けない
- 画面に埋め込むJSONは `_json_script()` を通す（手入力の自由記述に `</script>` が入りうる）。
  カードの描画側は全項目 `esc()` を通す
- 既定の並びは **Python 側（`render()` の `sorted`）で確定**し、ROWS はその順で出力する。
  JS はヘッダをクリックするまで並べ替えない（既定順の正しさをテストで固定できるようにするため）
- 日付一括バーは `cache` 付きの `bars_by_date()` で重複取得を避ける。
  銘柄別ループに入る前に `cache.clear()`（1日分 約4,447行 × 20日を抱えたままにしない）
- 営業日数は**土日のみ考慮・祝日未対応**（多めに出る）。画面にもそう書いてある

### 反証レビューで直したもの（2026-09-12・記録として残す）
実装直後に多角レビューを回し、**確認できた指摘だけ**を直した。特に効いたもの:
1. **「JSON行をコピー」が末尾カンマ付きだった** → 配列末尾に貼ると必ず不正JSONになり、
   `load_decisions` が `[]` を返して**最初の1件が黙って消える**。カンマを外し、
   さらに読み込みエラーを画面に赤字で出すようにした（ログだけに出して黙らせない）
2. **判断日が日足の取得範囲より前でも「実績確定」していた** → 1年半前の判断を
   直近の値動きで決済していた。左端ガードと「日足が古い」ガードを追加
3. **過去エピソードの「最大押し」が、安値が高値より前にあると起きていない下落を作っていた**
   → その時点までの高値からの下押しを走査する形に変更。5営業日後も**最大押しの日を起点**に
4. **エピソードの連鎖判定が直前のS高基準だった** → 15本間隔でS高が続く銘柄が永久に1件になる。
   エピソード開始からの間隔で数える形に変更
5. **セクター連動が自分自身を分子と母数に数えていた** → S高銘柄は必ず上昇側なので1本水増しされる
6. **判断ログの追加取得がコード文字列順だった** → 記録が増えると新しい判断が永久に実績待ちになる。
   「その銘柄の最も新しい判断日」の降順に変更
7. **信用残の「前週比」が週の抜けを無視していた** → ちょうど1週前の週とだけ比べる
8. 前日比・株価を調整済みに統一（上記の 14470 の件）。見送りの損益に良し悪しの色を付けない。
   前日比ちょうど0の日を「反発日」に数えない

**17種類の変異注入**（上記すべてを含む）が、テストで検知されることを確認済み。

### テストが届いていない範囲（正直に記録）
- **`_TABLE_JS` の `passes()`（フィルタの通過判定）と `cmp()`（ヘッダクリック後の並べ替え）は
  1行も実行されていない**。この環境に node が無く、CI も python しか入れていないため。
  既定表示は Python 側の並びをそのまま使うので影響は「ユーザーが操作した後」に限られるが、
  フィルタの不等号を反転させてもテストは通る。node を足すか、
  期待表を JSON で持って任意実行のテストにするのが次善策
- `index.html` は **90銘柄で約3.5MB**（98%がチャート系列）。`showChart` が凍結なので
  遅延読み込みにしていない。母集団がさらに増えるなら `docs/data/charts.json` へ切り出す案がある

---

※ 本ツールの抽出は機械的処理であり投資助言ではない。最終判断・発注は本人が行う。
